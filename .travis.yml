# Angular and Meteor - The perfect stack https://www.angular-meteor.com/
#
# Author: Georgy Berdyshev
#

sudo: required
services:
  - docker
env:
  - distribution: ubuntu
    version: bionic
before_install:
  - curl -fsSL https://download.docker.com/linux/ubuntu/gpg | sudo apt-key add -
  - sudo add-apt-repository "deb [arch=amd64] https://download.docker.com/linux/ubuntu $(lsb_release -cs) stable"
  - sudo apt-get update
  - sudo apt-get -y install docker-ce
  - docker --version
  - 'sudo docker pull ${distribution}:${version}'
  - 'sudo docker build --no-cache --rm --file=Dockerfile --tag=angular-meteor:latest .'
install:
  - "sudo docker run -v ${TRAVIS_BUILD_DIR}:/angular-meteor/ --user docker --workdir /angular-meteor angular-meteor:latest bash -c 'source $HOME/.nvm/nvm.sh; git clean -fXd; npm cache verify; npm install -g concurrently'"
script:
  - 'sudo docker exec -v ${TRAVIS_BUILD_DIR}:/angular-meteor/ --user docker --workdir /angular-meteor angular-meteor:latest /angular-meteor/run_tests.sh'
